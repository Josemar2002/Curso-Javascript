//Matématica no JS
let novoValor = Math.floor(Math.random() * 100)
console.log(novoValor)

/*
let novoValor = Math.round(3.5) = arredonda o número pra cima aparte de (.5) a abaixo que isso arredonda para baixo (.1, .2, .3 , .4)

let novoValor = Math.floor(3.5) = essa sempre vai arrendondar para baixo

let novoValor = Math.ceil(3.5) = sempre vai arredonadar para cima

let novoValor = Math.abs(3.5) = para sabe o numero absoluto original 

let novoValor = Math.min(3, 3, 45.5 ,1) = vai te retorna o menor número 

let novoValor = Math.max(3, 3, 45.5 ,1) = vai te retorna o maior número 

let novoValor = Math.random() = vai retorna um número aleatório entre zero e 1

let novoValor = Math.floor(Math.random() * 100) = vai gerar números aleatório de  1 ate 100
esse padrão de número pode (mudar de acordo com número que for multiplicado) 



*/